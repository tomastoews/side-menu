{
  "name": "side-menu",
  "version": "1.0.0",
  "scripts": {
    "dev": "parcel src/index.html",
    "build": "rm -rfv dist/ && parcel build src/index.html && node rewriteURLs.js",
    "build-and-deploy": "rm -rfv dist/ && parcel build src/index.html && node rewriteURLs.js && ./deploy.sh"
  },
  "keywords": [],
  "author": "Tomas Töws",
  "license": "ISC",
  "devDependencies": {
    "@parcel/transformer-sass": "^2.4.0",
    "autoprefixer": "^10.4.4",
    "parcel": "^2.4.0",
    "replace-in-file": "^6.3.2",
    "sass": "^1.21.0"
  }
}
